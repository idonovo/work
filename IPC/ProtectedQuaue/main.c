
#include "queue.h"
#include "producers.h"
#include "consumer.h"

int main()
{
	/*Queue* questionsQueue;
	Queue* answersQueue;
	Consumers* consumers;
	Producers* producers;
	void* consumerContext;
	void* producerContext;
	
	questionsQueue = QueueCreate(5);
	answersQueue = QueueCreate(5);
	
	consumers = CreateConsumers(1);
	producers = CreateProducers(1, 1);
	
	consumerContext = CreateConsumersContext(questionsQueue, answersQueue, consumers);
	producerContext = CreateProducerContext(questionsQueue, answersQueue, producers);
	
	ExecuteConsumerThreads(consumerContext);
	ExecuteProducerThreads(producerContext);
	
	ConsumersJoin(consumers, consumerContext);
	ProducersJoin(producers, producerContext); */
	
	return 0;
	
}

